# Voice Agent dependencies
# STT (Apple Silicon / MLX)
mlx-qwen3-asr>=0.1.0
faster-whisper>=1.0.0

# TTS
kokoro>=0.9.2
soundfile>=0.12.1

# Audio I/O
sounddevice>=0.4.6
numpy>=1.24.0

# LLM (OpenRouter uses the standard HTTP API)
requests>=2.31.0
